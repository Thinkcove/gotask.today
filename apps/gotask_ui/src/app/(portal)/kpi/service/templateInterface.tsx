export interface Template {
  id: string;
  title: string;
  description?: string;
  measurement_criteria: string;
  frequency?: string;
  status: string;
  createdAt?: string;
  updatedAt?: string;
}
